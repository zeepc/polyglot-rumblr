
<h1><%= @specific_profile.first_name %>'s Blog</h1>


<% @post = Post.where(user_id: @specific_profile.id)%>
<% @post.each do |post| %>
<div class="container blog">
	<div class="postbox jumbotron-fluid">
	<h3><%= post.title %></h3>
	<br>
	<p><%= post.body %></p>
</div>
</div>
<% end %>
